import type { ImportMap } from "payload";

// Generado automáticamente por `bun payload generate:importmap`
// Este archivo se actualiza cada vez que se agregan nuevas collections o vistas personalizadas
export const importMap: ImportMap = {};
